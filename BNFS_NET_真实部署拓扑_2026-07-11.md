# BNFS-NET 真实部署拓扑

**部署日期**：2026-07-11
**职责边界**：server3 为 CA Web + index；server4 为公网 relay；本机模拟 NAT 后的 server 与 client。

## 拓扑

```text
本机 natClient1/2 ─┐
                    ├── server4 relay :9000 ── server3 index :9010
本机 natServer1/2 ─┘             │                    │
                                CA Web ──────────────┘
                                server3 :9000
```

| 位置 | 常驻服务 | 监听 / 上游 |
|---|---|---|
| server3 (`129.28.42.247`) | `caserver`、`nodeserver -mode index` | CA Web `:9000`；index `:9010` |
| server4 (`193.112.62.9`) | `nodeserver -mode relay` | 公网 relay `:9000`；注册到 server3 `:9010` |
| 本机 | `httpfileserver` ×2、`tunserver` ×2、`tunclient` ×2 | server 目标 `127.0.0.1:19274/19275`；client 入口 `127.0.0.1:18881/18882` |

所有 NAT 节点固定经 server4 `193.112.62.9:9000` 接入，CA 地址为
`http://129.28.42.247:9000`。2026-07-12 起 server3 index、server4 relay 和 NAT 测试节点均不再设置
`BNFS_DISABLE_KCP`，默认并发建立 KCP + TCP 两条腿；KCP 未在完整握手期限内成功时才补第二条 TCP。

## 身份与余额

本机受限凭据文件 `.credentials/BNFS_NET_生产节点凭据_2026-07-11.json` 包含：

- `natServer1`、`natServer2` 私钥；
- `natClient1`、`natClient2` 私钥；
- 每个身份的 CA 余额均已校准为 `209,715,200B`（200MB）。

该文件及本机运行时私钥目录均为 `0600` / `0700`，并被 `.gitignore` 排除；不得提交或复制到服务器。
