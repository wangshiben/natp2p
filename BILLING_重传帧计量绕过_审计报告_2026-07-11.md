# BNFS-NET 流量计费审计报告：重传帧计量绕过

**日期**：2026-07-11（Asia/Shanghai）  
**范围**：P2P relay、NAT 注册/建连、数据面转发与 relay 侧结算；不审计 CA Web 的签发、充值、鉴权和账本业务逻辑。  
**原始结论**：本报告最初记录了 1 个已在真实沙箱验证的高危计量绕过，以及 1 个需要网络故障前提的结算丢账缺陷。当前 `admission=enforce` 下，未发现能让 **0 余额** 节点绕过连接保证金、完成 100MB 传输的已知 TOCTOU 路径。

**修复复核结论（2026-07-11）**：`BILL-RT-01` 的当前实现已封堵；未发现仅靠把新业务帧伪装成 `Retransmit` 就能在健康 relay→CA 链路上免除 server 上行结算的路径。但 `BILL-SET-02` 仍存在，另有同一节点对连接保证金的处理错误、连接窗口复用和后置结算信用敞口等问题，详见第 6 节。

## 1. 执行摘要

| 编号 | 问题 | 结果 | 与 5.05MB 入场费的关系 |
|---|---|---|---|
| BILL-RT-01 | 伪造 `FrameTypeRetransmit` 逃过 server 上行计量 | **历史真实 100MiB + SHA256 已验证；当前实现已修复** | 当前首见 `Data/Retransmit` 均计量，只有完全一致的近期重复帧免计 |
| BILL-SET-02 | `/settle` 通信失败后，已取出的待结算字节被永久遗忘 | 代码确认，未对线上 CA 链路做破坏性验证 | 同样可使入场费之外的字节费用丢失，但需要诱发 relay→CA 请求失败 |
| BILL-DEP-03 | 连接保证金 TOCTOU | **当前代码已封堵** | 0 余额并发请求全部拒绝；未形成 0 成本建连 |
| BILL-DEP-05 | `clientID == serverID` 时连接保证金双扣被覆盖 | 代码确认；属于原排除的 CA 账本后续项 | 余额需先满足 client 费门槛，但实际仅保留 server 费扣减 |
| BILL-DEP-06 | 同一节点对的 60 秒窗口复用任意新 `connID` | 代码确认 | 首次付款后可建立任意数量的新业务连接，削弱连接 churn 防护 |
| BILL-CR-07 | 10 秒后置结算且余额允许负值 | 代码确认 | 可在熔断前形成未担保的突发上行信用敞口 |

严格按“连 5.05MB 都不能支付”定义，当前未确认新的 0 余额建连漏洞。按本次约定将 5.05MB 视为正常入场成本，历史 `BILL-RT-01` 已不再成立；但 `BILL-SET-02` 和后置结算信用敞口仍可能造成已转发流量没有被有效收取。

## 2. BILL-RT-01：重传帧可承载新数据而不计量

**严重性**：高危（计费完整性）  
**前置条件**：攻击者控制一个已通过准入、可支付一次入场费的 server NatNode，以及一个 client NatNode；无需控制 relay、CA 或新增服务器。

### 根因

`networkFrameWork/forward_hook.go` 只把 `FrameTypeData` 计入 `TotalBytes`，将所有 `FrameTypeRetransmit` 直接跳过。可是接收协议并没有把 retransmit 限定为“此前已见的相同数据帧”：

1. `networkFrameWork/TcpStream.go` 对 `FrameTypeData` 与 `FrameTypeRetransmit` 均调用 `handleData`；
2. `network/Frame.go` 的 `FrameAssembler.Add` 也接受两种帧型，并可组装、交付完整 `Message`；
3. `networkFrameWork/FrameRouteRegistry.go` 的 relay→client pump 在调用计量 hook 后照常转发该帧；
4. 攻击者控制源 NatServer 的实现，可在合法 E2E 会话中把新的、已正确加密的业务帧标为 `FrameTypeRetransmit`。

因此 relay 把它当数据转发，客户端把它当应用消息接收，计量 hook 却不累计字节。

### 真实沙箱验证

使用既有 server3 CA/index、server4 relay 和仓库 `.credentials/` 中的两个已充值沙箱身份；全程固定走既有 relay、`BNFS_DISABLE_KCP=1`，未部署新服务。

1. 两个 NAT 节点正常申请 server/client 角色证书并注册到 server4 relay；
2. 正常建连，CA 原子扣除一次 client `5,242,880B`、server `51,200B`；
3. 受控 server 在该已建立的 E2E 会话中发送 `104,857,600B`，每个业务帧保留有效密文与消息格式、但帧型设为 `FrameTypeRetransmit`；
4. client 完整接收并计算 SHA256；等待超过 relay 的 10 秒结算周期后查询账本。

**观察结果**：

| 检查项 | 结果 |
|---|---|
| server→client 有效载荷 | `104,857,600B`（100MiB） |
| SHA256 | 一致 |
| client 账本变化 | `5,242,880B` |
| server 账本变化 | `51,200B` |
| server 上行按字节结算 | `0B` |

上述两项扣费完全等于设计内的连接保证金；没有发生任何与 100MiB 上行对应的 `settle` 扣减。测试结束后已恢复两个沙箱账户的初始余额，并将现有 server4 relay 按原命令重启以清除测试注册流。

### 修复建议

不要仅按 `FrameType` 决定是否计量。relay 应以 `(来源节点、连接 ID、MessageId、SeqId、payload digest)` 建立有界去重表：

- 首次看到的业务载荷（无论标为 Data 或 Retransmit）均计入上行；
- 仅当 retransmit 与此前已计费的同一帧完全一致时才免重复计费；
- 帧型与加密内容的绑定应在发送端/协议层强制，禁止任意新帧伪装成 retransmit；
- 增加端到端回归：新 `Retransmit` 载荷必须被计量，真实重复帧不能重复计费。

## 3. BILL-SET-02：结算请求失败导致待结算量丢失

**严重性**：高危（需网络可用性攻击或故障前提）  
**验证状态**：代码确认；未通过中断生产 relay→CA 链路进行实测。

`p2pnode/impl/relaynode/billing_hook.go` 的 `settleOnce` 在调用 CA 之前执行 `takeUnsettled(nodeID)`。该函数立即推进 `settledBytes`；若随后 `settler.Settle` 返回网络错误、超时或非成功响应，代码只记录日志并 `continue`，没有把 delta 放回待结算队列。

这与代码注释“下片重试”不一致：下一轮看到的待结算增量已经是零，先前已转发的字节永久不再上报。攻击者若能反复造成 relay 到 CA 的结算失败，可使该时间片的 server 上行不计费；这不是 CA Web 业务漏洞，而是 relay 本地的确认顺序错误。

### 修复建议

- 将待结算数据设计为 `pending → in-flight → committed`，仅在收到 CA 成功裁决后推进 `settledBytes`；
- 失败时保留原 delta 并重试；为避免重复扣费，给每批结算配置持久化的批次 ID，并由账本提供幂等语义；
- 对持续结算失败采取明确策略：计费完整性优先时熔断，或限制可透支的本地累计上限，不能无限保守放行。

## 4. 已验证的防护

`TestConnDeposit_TOCTOU_FreePass` 已通过：模拟慢速且永远拒绝的 `/reserve` 下，32 个同 `(client, server)` 并发请求全部被拒，CA 仅收到一次预扣请求。当前 `reservePair` 的 follower 会等待 leader 的结果，未再把 pending 状态当作已付款。

跨中继 bridge-mux 接入最终仍复用目标 relay 的 `ListenTCPConnection`，其本地托管目标会执行角色边界与保证金检查；本次未发现通过 bridge 路径跳过该检查的可行链路。

## 5. 原始修复优先级

1. **P0：修复 BILL-RT-01**，它已证明可让 100MiB server 上行不产生按量扣费；
2. **P0：修复 BILL-SET-02**，避免 CA 短暂不可达直接演变为永久丢账；
3. **P1：为每个连接和帧型建立协议一致性审计日志**，便于检测异常 retransmit 比例；
4. **P1：为注册流关闭清理 `StreamGroup`**，本次探针发现失效 group 会阻碍同身份重注册；这主要影响可用性，但会干扰恢复与审计复现。

## 6. 修复复核（2026-07-11）

**方法**：对当前 `dev_local` 代码进行静态数据流复核，并运行重传计量、保证金并发与结算的本地定向测试；本轮没有连接或影响现网服务。CA Web 的签发、充值、鉴权和账本业务逻辑仍不纳入本次 P2P 漏洞评级，涉及它的结论单列为后续项。

### 6.1 BILL-RT-01 已修复

当前 `forward_hook` 对首见的 `FrameTypeData` 和 `FrameTypeRetransmit` 都累计 payload；只有 `(nodeID, connectionID, messageID, seqID, totalFrames, payload fingerprint)` 完全一致、未被 ACK 回收且仍在有界 TTL 内的近期重复帧才免计。载荷变化、超过免费重传次数、缓存失效或配额压力下均重新计量。

本地回归通过：`TestForwardHookCountsPayloadOnly`、`TestForwardHookRetransmitDedupeRequiresExactRecentFrame`、`TestGlobalRetransmitCacheSharedAcrossStates` 和 `TestGlobalRetransmitCacheConcurrent`。因此，原先“新业务帧只改成 Retransmit”这一单独攻击手法不再可用。

### 6.2 BILL-SET-02 仍存在：失败请求永久丢账

`takeUnsettled` 在发起 `/settle` 前把 `settledBytes` 推进到当前累计值；随后网络错误、超时或 relay 关闭均不会回滚或持久化该增量。下一轮的 delta 已为零，现有注释所称“下片重试”不会发生。Relay 的账户表也只存于内存，关闭流程未执行最后一次成功结算。

**影响**：攻击者若能利用正常故障窗口、诱发 relay→CA 不可达，或在 relay 重启前突发发送，可使这一时间片的已转发字节缺失于结算。该问题不依赖 CA Web 的业务鉴权。

**修复要求**：采用持久化的 `pending → in-flight → committed` 状态；仅在 CA 成功确认后提交本地进度；结算批次必须携带幂等 ID，失败或重启后重放未确认批次。持续失败时应熔断或限制未结算信用额度。

### 6.3 BILL-DEP-05：同账户双扣覆盖（CA 账本后续项）

当 reserve 的 client 与 server 为同一 NodeID 时，账本先写入 client 扣费结果，再用相同 key 写入 server 扣费结果，后一次覆盖前一次。它仍要求余额先达到 client 费门槛，但成功后实际只保留 server 费扣减。

**修复要求**：显式拒绝同一账户承担 client/server 两个角色，或在相同账户时一次性扣除两笔费用之和，并补充同账户回归测试。

### 6.4 BILL-DEP-06：保证金窗口可复用于新连接

保证金缓存 key 仅为 `(clientNodeID, serverNodeID)`，不含 `connID`；60 秒内任何新连接都会直接继承已付款结果。代码无法区分 dual-leg/同一次重试与攻击者故意创建的新逻辑连接。

**影响**：首次付款后，攻击者可在窗口内创建任意多条新连接，绕开该 5.05MB 入场费用于抑制连接 churn 的设计目标。该项不单独绕过 server 上行按量结算，但会显著降低资源滥用成本。

**修复要求**：将去重限定到同一 `connID` 的双 leg；重试应使用由 relay 签发、绑定原连接且短时有效的恢复令牌，不能仅按节点对放行。

### 6.5 BILL-CR-07：后置结算的未担保突发额度

计量只在累计到 256KiB 时进入账户，CA 仅每 10 秒裁决一次，且账本允许负余额后才返回 `Allow=false`。余额不足的 server 因而能在被 cutoff 前按链路带宽突发传输，随后留下负账并弃用身份。

**影响**：这不是 `FrameType` 绕过；是否构成经济上的白嫖取决于 CA 对负账、身份重领和追偿的策略。但在当前代码模型下，relay 没有硬性的本地未结算字节上限。

**修复要求**：改为预留可用字节/硬上限，或把每节点的未结算上行限制在已验证余额与可审计信用额度以内；余额无法覆盖时在转发前停止。

### 6.6 计费模型与实现不一致

`billing_hook.go` 文件开头的说明称双向均计费，但实际 `meteringHook` 仍只接受 `relay_to_clients`，`client_to_relay` 永远不扣费。若产品策略确实只计 server 上行，则应修正文档与测试；若意图改为双向收费，则当前实现遗漏了该修改。

### 6.7 当前优先级

1. **P0：BILL-SET-02**，修复确认顺序、持久化和幂等重试；
2. **P1：BILL-CR-07**，为未结算流量设置可执行的信用上限；
3. **P1：BILL-DEP-06**，仅把保证金复用限定为同一逻辑连接的冗余 leg；
4. **后续 CA 账本项：BILL-DEP-05**，修复同账户双扣并增加回归测试。
