# DHT 物理距离感知 Relay 实机部署验证报告

**验证日期**：2026-07-12（Asia/Shanghai）  
**验证结论**：通过，但记录两个需继续跟踪的非阻断问题：单次 RTT 探针抖动较大；5MiB 应用载荷对应的 server 上行计量明显偏高。

## 1. 验证范围

本轮验证覆盖：

1. DHTable 健康状态、TTL/LRU 维护与只读质量排序回归；
2. NAT 节点按“RTT 档位 → 连续在线时间 → 负载 → RTT → XOR”自动选择入口；
3. server3 的 Index/Relay 与 server4 Relay 同时参与质量排名；
4. Relay 重启更换 NodeID 后，同一公网地址的旧 DHT 身份回收；
5. 本机 NAT Server/NAT Client 经真实公网 Relay 完成 5MiB 文件传输和 SHA-256 校验；
6. KCP/TCP 共存、准入计费、余额恢复与测试现场清理。

本轮不验证 Relay 故障后的在线迁移。当前入口只在 Bootstrap 时选择，既有连接不会因排名变化主动迁移。

## 2. 实际拓扑

```text
本机 NAT Server / NAT Client
        │
        ├── server3 129.28.42.247:9010  Index + Relay
        │          129.28.42.247:9000  CA Web（未重启）
        │
        └── server4 193.112.62.9:9000  Relay
                         │
                         └── 控制链路注册到 server3:9010
```

NAT 测试进程未传 `-relay`，设置 `BNFS_RELAY_INCLUDE_INDEX=1`，因此 server3 与 server4 使用同一质量规则参与选择。该开关默认关闭，不改变生产默认的“子 Relay 优先、Index 只作 fallback”语义。所有节点均未设置 `BNFS_DISABLE_KCP`。

## 3. 代码补充

### 3.1 Index 参与质量排名

- `relaySelector.includeIndex` 控制 Index 是否进入探测候选；
- `relaySelection.SelectedIndex` 区分“Index 正常排名胜出”和“全部候选不可达后 fallback”；
- 日志增加 `role=index|sub-relay`，避免把正常选中 Index 误报成故障回退；
- 默认不启用，实地验证通过 `BNFS_RELAY_INCLUDE_INDEX=1` 显式打开。

### 3.2 同地址旧 Relay 身份回收

第一轮实机部署发现：`nodeserver` 当前没有持久化 Relay 私钥参数，每次重启会生成新 NodeID。Index 原来会把同一 `IP:port` 的旧、新 NodeID 同时留在 DHT，查询一度返回 3 条记录；NAT 端虽然按地址去重后仍会连接正确地址，但可能使用旧身份的连续在线时间，污染稳定性排序。

修复后，Index 收到 Relay HELLO 时把公网地址视为唯一所有权：若相同非空地址已属于另一个 NodeID，则原子清除旧身份的地址索引、在线状态和 DHT 条目，再登记新身份。

实机两次观察到预期日志：

```text
Relay 地址身份已更新: addr=193.112.62.9:9000 old=332dab... new=cb235c...
Relay 地址身份已更新: addr=193.112.62.9:9000 old=cb235c... new=dfc1e8...
```

修复前查询返回 `3` 条（Index + 同地址旧、新 Relay）；修复后 NAT Server 与 NAT Client 均确认只返回 `2` 条（Index + 当前 Relay），且选择日志中的 NodeID 与 server4 当前身份一致。

## 4. 网络距离实测

### 4.1 ICMP RTT

每端 5 次，无丢包：

| 目标 | min | avg | max | mdev | 丢包 |
|---|---:|---:|---:|---:|---:|
| server3 | 56.256ms | 57.447ms | 59.087ms | 1.187ms | 0% |
| server4 | 42.554ms | 44.498ms | 46.154ms | 1.440ms | 0% |

### 4.2 TCP connect RTT

每端 15 次，使用与选择器相同的 TCP 建连口径：

| 目标 | min | median | avg | max | 成功率 |
|---|---:|---:|---:|---:|---:|
| server3 `:9010` | 58.037ms | 62.041ms | 63.397ms | 74.394ms | 15/15 |
| server4 `:9000` | 42.081ms | 48.909ms | 48.690ms | 54.681ms | 15/15 |

本机当前到 server4 更近：TCP 中位数比 server3 低 `13.132ms`。因此“本机距离 server3 更近”的假设在本次线路和时段下不成立。

### 4.3 RTT 档位行为

预验证曾出现：NAT Server 单次探测 server3 为 `55.119525ms`，两端落入同一最优 `10ms` RTT 档，server3 因连续在线时间更长而正常胜出；另一 NAT Client 单次探测 server4 为 `38.85629ms`，选择 server4。这证明算法不是机械选择绝对最小 RTT，而是按既定规则在同档内优先稳定节点。

最终复测中，NAT Server 和 NAT Client 都选择 server4：

| 节点 | 候选数 | 选中地址 | 单次探针 RTT | 角色 |
|---|---:|---|---:|---|
| NAT Server | 2 | `193.112.62.9:9000` | 213.991492ms | sub-relay |
| NAT Client | 2 | `193.112.62.9:9000` | 89.328159ms | sub-relay |

单次探针显著高于 15 次采样中位数，说明启动瞬间存在排队或调度抖动。最终结果仍正确，但后续建议用 2～3 次短探测的中位数，或复用短期 RTT 历史，降低单样本误判概率。

## 5. 文件传输验收

### 5.1 修复前现场样本

旧 Relay 身份仍存在时，地址去重保证连接仍落到 server4；5MiB 文件在 `140.192s` 完成，源端与接收端 SHA-256 同为：

```text
edb116c8fea8abd6aea476b401430783d913267beea3b3821e44f9652cb32779
```

旧 NodeID 仅影响元数据解释，不能据此认定其导致吞吐下降，因此该耗时只作为现场样本，不建立因果关系。

### 5.2 修复后最终样本

| 项目 | 结果 |
|---|---|
| 文件大小 | 5,242,880B（5MiB） |
| 用时 | 42.080s |
| 平均应用吞吐 | 约 121.7KiB/s |
| 源端 SHA-256 | `5f452d955b0e91da6825a7ea471cd6c17bbd980ab876a25340527f84df1bcf46` |
| 接收端 SHA-256 | `5f452d955b0e91da6825a7ea471cd6c17bbd980ab876a25340527f84df1bcf46` |
| 完整性 | 通过 |

NAT 本地日志没有 `retransmit`、`timeout` 或业务错误。最终 server4↔server3 控制链路在 Index 同时出现 `[::]:9010` 的 KCP leg 和 `10.6.0.16:9010` 的 TCP leg，二者使用同一 `connId`，KCP/TCP 共存成立。部署过程中也观察到一次 KCP 未在握手窗口完成后自动补 `tcp#2` 的双 TCP fallback，回退路径可用。

## 6. 计费观察

最终样本中：

- Client 保证金扣减 `5,242,880B`；
- Server 保证金扣减 `51,200B`；
- Server 文件传输结算累计 `12,342,846B`；
- Server 总扣减 `12,394,046B`；
- billing cache 最终观测 `hit=0`、`mismatch=0`、`repeat_charged=0`、`quota_drop=0`、`eviction=0`。

缓存没有显示重复帧再次计费，说明 BILL-RT-01 的精确去重路径未出现绕过迹象。但 `12,342,846B / 5,242,880B = 2.354x`，独立帧计量明显高于应用有效载荷。当前证据不能把它归因于 DHT；建议单独审计帧大小自适应、协议头、ACK/控制帧归属和 dual leg 是否生成不同序列的独立数据帧，并建立“应用字节 → 唯一数据帧字节 → CA settle 字节”的分层对账。

## 7. 自动化回归

以下竞态回归通过：

```bash
GOCACHE=/tmp/bnfs-go-cache go test -race \
  ./p2pnode/impl/natnode \
  ./p2pnode/impl/relaynode \
  ./p2pnode/relayquery \
  ./DHTable -count=1
```

结果：

```text
ok bnfs_p2p/p2pnode/impl/natnode   3.924s
ok bnfs_p2p/p2pnode/impl/relaynode 106.267s
ok bnfs_p2p/p2pnode/relayquery     1.013s
ok bnfs_p2p/DHTable                34.992s
```

新增定向测试覆盖：默认排除 Index、显式包含 Index、Index 正常胜出与 fallback 状态区分、全部不可达回退、同一 Relay 地址更换身份后旧 DHT 条目清理。

## 8. 部署与回滚

两机最终 `nodeserver` SHA-256：

```text
9dfba3676f6ad5488c9faa1d3768eef89fbf54ea323de95dff7743b18d33360e
```

| 主机 | 最终服务 | 回滚二进制 |
|---|---|---|
| server3 | CA Web `:9000` + Index TCP/UDP `:9010` | `/root/bnfs-net/nodeserver.pre-dht-v2-20260712-120106` |
| server4 | Relay TCP/UDP `:9000` | `/root/bnfs-net/nodeserver.pre-dht-v2-20260712-120109` |

server3 的 CA PID 始终为 `955858`，本轮未重启 CA/账本。最终 server3 Index PID 为 `2482938`，server4 Relay PID 为 `4998`；最终 Relay 身份前缀为 `dfc1e88d...`，已成功注册到 Index。

## 9. 收尾状态

- 本机 NAT Server、NAT Client、HTTP 测试进程均已停止，测试监听端口已释放；
- server4 已冷重启，测试遗留的 NAT StreamGroup 已清空；
- Index 已把 server4 的旧身份替换为最终身份；
- 四个本机测试身份余额均精确恢复为 `209,715,200B`；
- 私钥未写入报告或远端服务器，运行时私钥文件仍为 `0600`；
- 原始日志和下载证据位于 `.credentials/dht-live-20260712/` 与 `.credentials/dht-live-20260712-v2/`，均被 `.gitignore` 排除。

## 10. 验收结论

本轮 DHT/RelaySelector 实机部署通过：质量候选可解释、Index 可控参与、当前线路正确选择 server4、Relay 地址重绑定不会再遗留旧身份、KCP/TCP 共存、文件完整性和回退路径均通过。

后续优先级建议：

1. P1：对 `2.354x` 唯一帧计量放大做分层对账，确认是否存在业务计费偏差；
2. P2：把单候选一次 TCP connect 改为少量采样中位数或短期 EWMA；
3. P2：为 `nodeserver` 增加持久化 `-key`，减少 Relay 重启时 NodeID 变化；
4. P2：修复注册双腿全部断开后的僵尸 StreamGroup 生命周期，避免依赖冷重启清场。

## 11. XOR 逻辑近、RTT 物理远的定向对照

按以下方式构造用户指定场景：

| 角色 | 实际节点 | 测试时 NodeID |
|---|---|---|
| 本机 NAT | `natServer2` | `5e7386dce6b1d286...` |
| serverN（逻辑近） | server3 `129.28.42.247:9010` | `19f33f19505b0508...` |
| serverX（RTT 近） | server4 `193.112.62.9:9000` | `dfc1e88de3185335...` |

`natServer2` 到两个 Relay 的 256 位 XOR 距离为：

```text
d(natServer2, serverN) = 4780b9c5b6ead78edaa4fd1baefacabae3d1dafcb5adfd9a794158246f2fc4fa
d(natServer2, serverX) = 81b26e5105a981b30ec0fd0b07f74077e1a1182545b057dd06a26198352e38e3
```

无符号整数比较满足 `0x47... < 0x81...`，所以 serverN 在 DHT 逻辑距离上严格更近。

同一时段各做 9 次 TCP connect 采样：

| 目标 | median | avg |
|---|---:|---:|
| serverN | 63.785ms | 62.832ms |
| serverX | 48.140ms | 47.450ms |

serverX 的 TCP 中位 RTT 比 serverN 低 `15.645ms`，物理网络距离更近。随后本机使用 `natServer2`
固定私钥、不传 `-relay`，仅设置 `BNFS_RELAY_INCLUDE_INDEX=1` 运行真实 Bootstrap，结果为：

```text
index 129.28.42.247:9010 返回 2 个 relay
[relay-select] candidates=2 selected=dfc1e88de3185335 addr=193.112.62.9:9000
role=sub-relay rtt=43.237202ms band=0 reason=best-band,longest-stable
注册到 relay: 193.112.62.9:9000
```

结论：即使 serverN 的 XOR 逻辑距离更近，RTT 已跨档时仍由 serverX 胜出，符合“网络质量优先、XOR
仅作同质量条件下的最终确定性决胜”设计。注册前后 `natServer2` 余额均为 `209,715,200B`，探测和注册
未产生保证金或流量扣费。

测试后已停止本机进程并冷重启 server4 清除注册内存组。server4 最终 NodeID 更新为 `aa91fa0e...`；
此时 `natServer2` 到 serverX 的 XOR 距离以 `f4...` 开头，仍大于到 serverN 的 `47...`，因此当前
部署依然保持“serverN 逻辑近、serverX RTT 近”的构造前提。原始证据位于
`.credentials/dht-xor-vs-rtt-20260712/`。
