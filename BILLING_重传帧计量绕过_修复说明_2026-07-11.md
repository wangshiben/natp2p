# BILL-RT-01 重传帧计量绕过修复说明

**状态**：已在代码与回归测试中修复，尚未部署到实地 relay。
**关联审计**：`BILLING_重传帧计量绕过_审计报告_2026-07-11.md`

## 修复策略

relay 不再将 `FrameTypeRetransmit` 一律排除在计量外。每个被托管 server 的
`StreamGroup` 维护一个有界的帧指纹表，键为：

```text
(connection ID, message ID, sequence ID, total frames, SHA-256(payload))
```

- 首次看见的 `Data` 或 `Retransmit` 都计入净荷；因此新载荷伪装为重传不会逃费。
- 只有指纹完全一致、且仍在该连接最近两个有效 ACK 周期内的帧才不重复计量。
- relay 仅保存固定长度摘要，不缓存业务载荷；每个 group 最多保留 65,536 条记录。
- ACK 周期到期、连接关闭或缓存受限淘汰时，后续帧会重新计量。这最多造成保守计费，不会造成漏计。

## ACK 与边界

仅当 client→server 方向存在已建立反向路由、且 ACK range 可解析时，relay 才推进对应连接的
ACK 周期；无路由 ACK 不会新建路由或影响缓存淘汰。这样任意伪造 ACK 不能制造新的免费窗口。

正常重传的原始首帧若在到达 relay 前丢失，relay 首次看到的可能就是 `Retransmit`；该帧会按首见
业务数据计量并转发，避免用“拒绝未知重传”换取可用性回归。

## 覆盖

`TestForwardHookRetransmitDedupeRequiresExactRecentFrame` 覆盖：真实重复帧不重复计量、相同
序号但不同载荷的伪造重传必须计量、两个 ACK 周期内可去重、超过窗口重新计量。

## 方案评估：Relay 性能、安全与弱网体验

当前“两个 ACK 周期 + SHA-256 帧指纹”方案能够封堵 BILL-RT-01，但需要 relay 对每个业务帧
计算摘要，并为每个 `StreamGroup` 保存有界指纹表。它更接近应用 goodput 计费，却不是 relay
性能优先场景下的唯一选择。

| 方案 | Relay CPU / 内存 | 安全性 | 弱网 NatServer 影响 | 结论 |
|---|---|---|---|---|
| Data + Retransmit 全部计费 | O(1) 计数；无逐帧缓存 | 严格，无帧型绕过收益 | 重传字节也扣费 | Relay 性能与安全最佳 |
| 有上限的免费重传额度 | O(1)；每节点少量计数器 | 漏费比例有严格上限 | 小比例重传免费 | 推荐折中 |
| keyed 帧指纹精确去重 | 每帧摘要；O(帧数) 有界状态 | 高 | 对弱网最公平 | Relay 成本较高 |
| relay 签发重传凭证 | 每帧 MAC；需要协议升级 | 高 | 公平 | 工程复杂度高 |
| 禁止应用层重传 | O(1) | 高 | ACK 丢失或 failover 时可用性下降 | 不建议 |

### 方案 A：按 Relay 实际转发量计费

relay 对 `FrameTypeData` 和 `FrameTypeRetransmit` 的 payload 一视同仁，ACK 与控制帧仍不计费。
该方案不需要摘要、缓存或 ACK 关联，攻击者修改帧类型没有任何经济收益。

重传确实再次占用了 relay 出口带宽，因此这一口径可解释为“按实际资源消耗收费”。其代价由
弱网 NatServer 承担：100MiB 有效载荷在 1%、5%、20% 重传率下，分别约多计 1MiB、5MiB、
20MiB。它增加费用但不主动拒绝重传，不影响数据面正确性。

### 方案 B：有上限的免费重传 Token Bucket

若需要兼顾普通弱网，可为每个 NatServer 保存常数级状态：

```text
每计费 100B 正常 Data，产生 3～5B 免费重传 token
Retransmit 先消耗 token，余额不足部分按实际字节计费
token 最多累计 1MiB/连接或 4MiB/节点，并按结算周期衰减
```

token 只能由已计费的 `Data` 产生。若攻击者把所有新数据标成 `Retransmit`，没有 token 可用，
流量仍被全额计费；若先发送正常 Data 再伪造重传，最多只能获得配置比例的折扣。因此安全损失
从“任意量绕过”收敛为可计算的 3%～5% 上限。

该方案不需要逐帧摘要、ACK 跟踪或帧缓存，relay 只需维护 `dataBytes`、`retransmitBytes` 和
`retransmitTokens` 等少量整数。不能采用“所有重传固定打折”，否则攻击者仍可通过把全部新载荷
标记成重传获得无上限折扣。

## Keyed 帧指纹精确去重详解

### 目标与威胁模型

该方案保留“正常重传不重复计费”的 goodput 口径，同时不信任 NatServer 声明的 `FrameType`。
攻击者可控制 NatServer、NatClient、帧类型、消息编号和载荷，但不能获知 relay 进程内的随机密钥。

relay 对第一次看见的业务帧计费并保存不可预测的短指纹。后续 `Retransmit` 只有在逻辑身份、帧位置
和载荷指纹全部一致时，才可能被视为已计费副本。以下情况必须计费：

- 从未出现过对应原帧的 `Retransmit`；
- 使用新的 connection、message 或 sequence 编号；
- 复用原编号但改变 payload；
- 指纹已经过期或因容量限制被淘汰；
- 完全相同帧超过协议允许的免费重传次数。

### 指纹与索引

建议将查找键和载荷指纹分开：

```text
FrameKey = (
  hostedNodeID,
  direction,
  logicalConnectionID,
  messageID,
  sequenceID,
  totalFrames,
  payloadLength
)

Fingerprint = KeyedHash(
  relaySecret,
  canonical(FrameKey) || payload
)[0:16]
```

`FrameKey` 定位协议中的同一帧，128-bit `Fingerprint` 判断载荷是否完全一致。编码必须使用固定字节序
和长度前缀，不能直接拼接字符串，否则不同字段组合可能产生相同输入。

指纹算法可按性能预算选择：

| 算法 | 安全属性 | 性能与依赖 | 建议 |
|---|---|---|---|
| HMAC-SHA-256 截断 128-bit | 标准密码学 MAC | 标准库可用；每帧两轮 SHA 成本较高 | 安全基线 |
| keyed BLAKE3 截断 128-bit | 密码学 keyed hash | 吞吐高；需新增成熟依赖 | 高吞吐 relay 优选 |
| 双种子 keyed 64-bit hash 组成 128-bit | 防离线构造，碰撞概率低 | 最快；需严格审计实现 | 仅在基准证明 SHA/BLAKE3 成本不可接受时考虑 |
| 普通 CRC、FNV、xxHash | 非对抗性校验 | 快 | 不可用于敌对计费边界 |

不建议仅使用未加密的 64-bit checksum。攻击者可以离线构造碰撞或利用截断空间降低新载荷被识别的
概率。128-bit keyed 指纹即使暴露缓存行为，也无法预先计算可用碰撞。

### 判定状态机

```text
收到 Data 或 Retransmit
        │
        ├─ FrameKey 未命中
        │      → 计费
        │      → 保存 keyed fingerprint，freeRepeatCount=0
        │
        └─ FrameKey 已命中
               │
               ├─ fingerprint 不一致
               │      → 计费并记录 mismatch
               │      → 不覆盖原始 fingerprint
               │
               └─ fingerprint 一致
                      │
                      ├─ 未过期且 freeRepeatCount < 6
                      │      → 免重复计费，freeRepeatCount++
                      │
                      └─ 已过期或次数达到上限
                             → 重新计费
```

载荷不一致时不能用新指纹覆盖原记录。否则攻击者可以让 A/B 两种载荷交替成为“当前指纹”，扩大状态
扰动面。mismatch 帧每次都计费，必要时还可作为异常行为进入限速或断连策略。

### 为什么需要免费次数上限

仅靠精确指纹仍存在资源计费边界：攻击者可以无限重放完全相同的密文。官方接收端通常会按
`MessageId + SeqId` 丢弃重复帧，不会产生新的应用 goodput，但 relay 仍反复消耗出口带宽。

当前发送协议最多进行 6 次应用层重传，因此建议每个指纹最多免计 6 次。超过上限后可以选择：

1. 继续转发并按实际 payload 计费；
2. 对持续异常连接限速；
3. 重复比例显著异常时关闭连接。

默认应采用第 1 种，保证缓存异常不会直接损害数据面可用性；第 2、3 种作为独立的滥用治理策略。

### 生命周期：不依赖逐帧 ACK

为避免逐 ACK 维护带来的热路径开销，可改用有界 TTL：

- 指纹从首次计费或最近一次合法重传起保留 90 秒；
- 90 秒覆盖当前大多数 `800ms～8s` 退避与最多 6 次重传场景；
- 极端大消息在持续 ACK 进展下可能超过该窗口，届时真实重传会被重新计费，但不会产生漏计；
- 连接关闭立即清除其全部记录；
- relay 重启后缓存为空，后续重传按首次帧计费。

如果弱网实测显示 90 秒不足，可提高到 120 秒。TTL 只影响误重复计费概率，不影响安全性：缓存过早
失效的结果是多计，不能让新载荷免费通过。

### 缓存结构与 Relay 性能

不能为每个 `StreamGroup` 各分配 65,536 条记录，否则大量托管 NatServer 会使内存按节点数线性放大。
推荐 relay 进程级共享、分片的有界缓存：

```text
64 个 shard
每个 shard 独立锁与 map
全局最大 65,536～262,144 条记录
每 NatServer、每连接分别设置子配额
时间轮或分代桶执行 TTL 淘汰
```

每条记录建议只保存压缩后的节点/连接标识、帧位置、16B 指纹、过期时间和重复次数，不保存 payload。
若单条记录按约 80～120B 估算：

| 全局记录数 | 预计内存 |
|---:|---:|
| 65,536 | 约 5～8MiB |
| 131,072 | 约 10～16MiB |
| 262,144 | 约 20～32MiB |

实际值必须通过 Go benchmark 和 heap profile 确认。map、字符串和指针会产生额外开销，因此热路径中
应使用定长数值键，避免保存完整 NodeID / ConnectionID 字符串。

CPU 开销主要来自对 server→client 每个业务帧计算 keyed hash。以 1400B payload 估算，1Gbps 约为
每秒 9 万帧；必须分别测试单核吞吐、锁竞争、GC 和多连接扩展性。优化顺序建议为：

1. 只在计费方向 `relay_to_clients` 计算指纹；
2. 使用分片缓存避免全局锁；
3. 使用定长栈上编码，避免每帧分配；
4. 复用 hash 状态或采用支持 keyed 模式的高吞吐算法；
5. 容量满时直接淘汰并重新计费，不阻塞转发等待缓存空间。

### 两种缓存组织的 CPU 与内存预估

以下对比针对两种 keyed 指纹缓存组织：

1. 每个 `StreamGroup/NatServer` 使用独立缓存；
2. relay 使用全局分片缓存，并配置 NatServer/连接配额及批量 ACK range 回收。

按平均 payload 1400B 估算：

| 业务吞吐 | 业务帧速率 |
|---:|---:|
| 100Mbps | 约 8,900 帧/秒 |
| 500Mbps | 约 44,600 帧/秒 |
| 1Gbps | 约 89,300 帧/秒 |

单条缓存记录包含压缩后的帧身份、128-bit 指纹、过期时间和重复次数。考虑 Go map bucket、对齐和
索引后，预计占用 80～120B；最终值需通过 heap profile 确认。

#### 组织一：每 NatServer 独立缓存

独立缓存没有跨 NatServer 锁竞争，也不会发生高流量节点淘汰其他节点指纹的问题。优化到无逐帧
内存分配、O(1) 淘汰后，单核 CPU 初步估算为：

| 吞吐 | keyed BLAKE3-128 | HMAC-SHA-256 截断 128-bit |
|---:|---:|---:|
| 100Mbps | 约 2%～6% | 约 4%～10% |
| 500Mbps | 约 8%～20% | 约 15%～35% |
| 1Gbps | 约 15%～35% | 约 25%～60% |

多个 NatServer 可以分散到不同 CPU，但 Go GC、调度、map 扩容及大量独立过期队列会增加额外开销。

若每个 `StreamGroup` 上限为 65,536 条：

| 活跃 NatServer 数 | 预计指纹内存 |
|---:|---:|
| 1 | 约 5～8MiB |
| 20 | 约 100～160MiB |
| 100 | 约 500～800MiB |
| 1000 | 约 5～8GiB |

65,536 条仅能覆盖 100Mbps 下约 7.3 秒或 1Gbps 下约 0.73 秒的连续帧。若完全不解析 ACK、仅靠
90 秒 TTL，100Mbps 单节点需要约 80 万条记录（64～96MiB），1Gbps 单节点需要约 804 万条
记录（640～960MiB），生产上通常不可接受。

#### 组织二：全局分片缓存 + 配额 + 批量 ACK 回收

全局缓存固定 relay 总内存，但每帧除 keyed hash 和 map 查询外，还涉及 shard 锁、节点配额统计，
以及 ACK range 对应的批量删除。优化实现的单核 CPU 初步估算为：

| 吞吐 | keyed BLAKE3-128 | HMAC-SHA-256 截断 128-bit |
|---:|---:|---:|
| 100Mbps | 约 4%～10% | 约 6%～15% |
| 500Mbps | 约 12%～30% | 约 20%～50% |
| 1Gbps | 约 25%～55% | 约 40%～90% |

ACK 大约每 100 帧或空闲周期产生一次，但一个 ACK range 可能集中删除约 100 条记录，因此平均操作量
仍接近每帧一次删除，并会形成影响 P99 的突发。shard 必须按完整 `FrameKey` 分布；若仅按
ConnectionID 分片，一个高速连接仍会使单 shard 串行化。

| 全局记录上限 | 基础记录内存 | 加分片索引、时间轮后的预计值 |
|---:|---:|---:|
| 65,536 | 约 5～8MiB | 约 7～12MiB |
| 131,072 | 约 10～16MiB | 约 12～22MiB |
| 262,144 | 约 20～32MiB | 约 24～44MiB |

解析已有批量 ACK range 后，缓存规模主要取决于未确认窗口。RTT 200ms 时，100Mbps 理论在途约
1,800 帧（0.15～0.22MiB 指纹），1Gbps 约 18,000 帧（1.4～2.2MiB）。但若弱网 client 连续
8 秒没有有效 ACK，1Gbps 连接可能积累约 71 万条记录（57～86MiB），必然触发连接配额并降级。

为避免 noisy neighbor，必须设置每 NatServer 保护配额、节点最大占用和每连接子配额。节点超额时
优先淘汰自己的记录，不能淘汰其他节点的保护区；容量不足后只让该连接降级为 token bucket 或实际
字节计费。

#### 当前原型的额外瓶颈

当前代码中的 `recordFrame` 每次插入前都会遍历缓存执行 `pruneConnectionLocked`，因此实际复杂度
是 O(n)，不能直接部署。缓存已有 10,000 条、输入 100Mbps 时，可能接近每秒 9,000 万次 map
条目检查；达到 65,536 条时会更严重。

必须改成时间轮、分代桶或真正的 O(1) LRU 淘汰后，上述 CPU 估算才有参考意义。当前 server4 为
2 核配置，不建议让 keyed 指纹方案承担稳定 1Gbps；即使采用 BLAKE3，全局缓存也可能消耗半个核心
以上，HMAC-SHA-256 在锁竞争和 GC 叠加后可能接近占满一个核心。

第一版可用于基准测试的建议参数为：

```text
keyed BLAKE3-128
64 shards
全局上限 131,072 条（预计 12～22MiB）
每 NatServer 保护配额 4,096 条
每 NatServer 最大 32,768 条
每连接最大 8,192 条
完全重复最多免费 6 次
解析现有批量 ACK range 回收
容量不足时仅当前连接降级到 token bucket/实际字节计费
```

适用判断：节点数量少、单节点吞吐低于约 100Mbps 时，独立缓存更简单且隔离更好；节点较多或聚合
吞吐较高时需要全局分片与配额。若目标接近 1Gbps 且 relay 只有 2 核，token bucket 的性能和资源
可预测性明显优于 keyed 精确去重。

### 安全失败语义

该缓存必须遵循“缓存只负责减免费用，不能决定是否计费”的原则：

| 异常 | 行为 |
|---|---|
| cache miss | 计费并尝试写入 |
| cache full | 淘汰旧项；当前帧计费 |
| TTL 过期 | 当前帧重新计费 |
| relay 重启 | 缓存清空；重传重新计费 |
| fingerprint mismatch | 每次计费，并增加安全指标 |
| hash/canonical 编码异常 | 计费；不得免费放行 |

这种 fail-closed 仅作用于费用减免：缓存故障可能让 NatServer 多付费，但不会漏费，也不会因为缓存服务
不可用而停止数据转发。

### 多链路与跨 Relay 边界

- TCP/KCP dual-leg、双 TCP failover 必须共享同一个逻辑连接缓存，不能按物理 leg 分开，否则换腿后的
  正常重传会被重复计费。
- 指纹键使用业务 `ConnectionId`，不能使用 socket、远端端口或物理流 ID。
- 计费与指纹判定应只发生在托管 NatServer 的目标 relay；跨 relay bridge 不能在两端重复计费。
- 若同一 NatServer 可在多个 relay 同时注册，本地缓存无法跨 relay 去重。需要禁止并发托管，或接受
  跨 relay 重传重新计费；不建议为此引入同步式分布式逐帧缓存。

### 可观测性

至少应提供以下按 NatServer 聚合的指标：

- `fingerprint_cache_hit`：完全一致且免重复计费；
- `fingerprint_cache_miss`：首见帧并计费；
- `fingerprint_mismatch`：相同帧位置出现不同载荷；
- `fingerprint_expired` / `fingerprint_evicted`：过期或容量淘汰；
- `free_retransmit_bytes` / `charged_retransmit_bytes`；
- cache 当前条目数、命中率、hash 耗时与 shard 锁等待。

`fingerprint_mismatch` 和单连接重复次数超过 6 应作为高优先级审计信号，因为正常实现不会改变已发送
帧的密文内容。

### 必需测试

1. 首见 Data 正常计费，完全一致 Retransmit 不重复计费；
2. 首见就是 Retransmit 时必须计费；
3. 相同编号但 payload 不同必须每次计费；
4. connection/message/sequence/total 任一字段变化均视为新帧；
5. 第 1～6 次完全重复免费，第 7 次开始计费；
6. TTL 到期、容量淘汰和 relay 重启模拟后重新计费；
7. dual-leg/failover 下共用缓存，不因换腿重复计费；
8. 多连接并发与 `go test -race`；
9. 基准覆盖 100Mbps、1Gbps 和目标生产峰值，并记录 CPU、分配、GC 与 P99；
10. 真实弱网下核对应用 payload、relay 计量与 CA 扣费三方差异。

### 适用结论

Keyed 指纹方案适合“必须尽量按应用 goodput 收费，同时可以为 relay 支付一定 CPU/内存成本”的场景。
它比逐帧 ACK 状态更独立，也比普通 SHA 指纹更适合敌对输入，但仍明显重于全量计费和 token bucket。

在采用前应先完成目标机器上的吞吐基准。如果 keyed hash + 分片缓存使 relay CPU 增幅超过 10%，或
P99 转发延迟明显上升，应回退到方案 B；如果业务坚持精确去重，则建议 keyed BLAKE3-128、90 秒 TTL、
全局 131,072 条记录和每指纹最多 6 次免费重传作为第一版参数。

## 2026-07-11 实现结果

本次按“全局分片缓存 + 配额 + 批量 ACK 回收”完成了第一版实现。安全目标是修复 BILL-RT-01：攻击者
不能只伪造 `FrameTypeRetransmit` 就免费转发；只有 relay 先前见过并计费过、位置和载荷都完全相同的
帧，才允许有限次数免重复计费。

### 落地参数

| 参数 | 实际值 |
|---|---:|
| 全局 shard | 64 |
| 全局记录上限 | 131,072 |
| 每 NatServer 保护额度 | 4,096 |
| 每 NatServer 最大额度 | 32,768 |
| 每逻辑连接最大额度 | 8,192 |
| 指纹 | 随机进程密钥的 HMAC-SHA-256，截断 128 bit |
| TTL | 90 秒 |
| 同一精确帧最多免费重复 | 6 次 |
| 回收 | 成功转发批量 ACK range 后按连续前缀回收 |
| 异常语义 | miss/full/mismatch/TTL/重启均计费，数据仍转发 |

实现位于 `networkFrameWork/retransmit_cache.go`。一个 `ForwardHookConfig` 只初始化一个全局缓存，所有
`StreamGroup` 共享；分片锁按完整帧键分散，移除了旧原型插入时 O(n) 扫描。缓存键包含
`NatServer NodeID + ConnectionID + MessageID + SeqID + TotalFrames`，载荷用 keyed 指纹校验。
ACK 只有在已经成功转发给原发送端后才触发回收，连接关闭会清除该连接的缓存和 ACK 进度。

配额满、指纹不一致或第 7 次完全重复都采用 fail-closed：该帧按实际 payload 计费，不会因缓存故障
免费放行，也不会因为缓存不可用而中断正常业务。每 10 秒输出一次 `[billing-cache]` 汇总，当前包括
`entries/hit/miss/mismatch/repeat_charged/quota_drop/eviction/expired/ack_removed`。

### 自动化验证

以下回归均通过：

```bash
GOCACHE=/tmp/bnfs-go-cache go test ./networkFrameWork \
  -run 'TestForwardHook|TestGlobalRetransmitCache|TestTcpStream' -count=1
GOCACHE=/tmp/bnfs-go-cache go test -race ./networkFrameWork \
  -run 'TestGlobalRetransmitCacheConcurrent|TestForwardHookRetransmit' -count=1
GOCACHE=/tmp/bnfs-go-cache go test ./networkFrameWork ./p2pnode/impl/relaynode \
  ./cmd/tunnel/nodeserver ./cmd/tunnel/server ./cmd/tunnel/client -count=1
```

覆盖项包括：首见 Data/首见 Retransmit 计费、精确重传免费、同位置不同 payload 必须计费、免费 6 次
上限、ACK range 回收、跨 `StreamGroup` 全局共享、NatServer 隔离和并发竞态。

### 本机微基准

测试机 CPU 为 Ryzen 5 5500U，payload 约 1.4KiB：

| 路径 | 延迟 | 等效吞吐 | 分配 |
|---|---:|---:|---:|
| 首见帧写入 + 批量 ACK 回收 | 1,695 ns/op | 826.14 MB/s | 256 B/op，7 allocs/op |
| 精确重复命中 | 1,482 ns/op | 944.93 MB/s | 115 B/op，4 allocs/op |

该结果证明 O(n) 扫描已消除，但分配次数仍偏高。2 核 Relay 若要长期承载接近 1Gbps，仍应继续优化
键构造/HMAC 对象生命周期并补充 P99 与 GC pause 测试；当前数据不能解释成 1Gbps 生产容量承诺。

## 双 NatServer 竞争实地测试

### 拓扑与版本

```text
server3 129.28.42.247: CA Web :9000 + index :9010
server4 193.112.62.9: relayServer :9000（2 vCPU / 1,966.9 MiB RAM）

本机 Pair A: 正常 NatServer1 -> Relay -> 弱网 NatClient1
本机 Pair B: 弱网 NatServer2 -> Relay -> 正常 NatClient2
```

server4 部署的 `nodeserver` SHA-256 为
`0fc318079a64f100ff02a33f14c413af8cd0e515b641dc3796b4220c68ca7234`，旧二进制备份为
`/root/bnfs-net/nodeserver.pre-rt-cache-20260711`。两组均禁用 KCP，使用双 TCP failover 并竞争同一个
Relay。CA/Index 沿用 server3 的现网服务，没有把它们错误地部署到 Relay 主机。

弱网注入采用测试专用环境变量，不进入生产默认路径：

- Pair A 的 NatClient 对每个 ACK 在写出前主动 `sleep 900ms`，略高于当前 `minAckTimeout=800ms`，
  用来稳定制造 NatServer 重传和最坏 ACK head-of-line；
- Pair B 的 NatServer 对每个 Data/Retransmit 帧写出后主动 `sleep 2ms`，用来限制服务端上行速率；
- `BNFS_TEST_LOG_RETRANSMIT=1` 只增加测试日志，不改变重传判定。

`900ms/ACK` 是故意偏激的上界压力模型，不等价于普通 900ms RTT：逐 ACK 串行 sleep 会放大队头阻塞，
因此适合验证隔离和失败语义，不应拿它代表一般移动网络的平均体验。

### 第一轮：两条 5MiB 同时下载

测试前（首次连接保证金已经扣除）余额：两台 NatServer 均为 `209,664,000B`，两台 NatClient 均为
`204,472,320B`。结果如下：

| 场景 | 应用接收 | 用时 | 应用速率 | 结果 |
|---|---:|---:|---:|---|
| Pair A：正常 Server + 900ms ACK Client | 524,082B / 5MiB | 120.001s | 4,367B/s | curl 超时 |
| Pair B：2ms/帧 Server + 正常 Client | 5,242,880B | 20.962s | 250,114B/s | 成功，SHA-256 一致 |

Pair A 的 NatServer 日志记录到 76 次重传事件、共 7,161 帧/5,710,628B 的重传构造；这些帧不保证
全部成功到达 Relay。Relay 的全局命中计数增加 110，证明有精确重复被免重复计费。该链路最终耗尽
重传次数、发生 TCP leg 重连；因为跨过 60 秒保证金窗口，NatClient 额外扣 `5MiB`，NatServer 额外
扣 `51,200B` 连接保证金。

第一轮 CA 扣费拆分：

| 节点 | 应用有效载荷 | 流量结算 | 额外连接保证金 | 流量/有效载荷 |
|---|---:|---:|---:|---:|
| NatServer1 | 524,082B（部分响应） | 1,575,508B | 51,200B | 3.006x |
| NatServer2 | 5,242,880B | 6,827,868B | 0 | 1.302x |

这里没有少计费或 BILL-RT-01 免费绕过；相反，ACK 已回收、TTL 已过期或换 leg 后才到达的帧按
fail-closed 重新计费，所以极端弱网会出现费用放大。

### 第二轮：同步采集 Relay 资源

第二轮把 Relay 的 0.2 秒采样与下载同步启动。Pair A 的上一请求仍处于重传/重连恢复状态，新请求在
30.003 秒内收到 0B；Pair B 仍在 21.558 秒完成 5MiB，SHA-256 一致，速率 243,199B/s
（约 237.5KiB/s）。这说明高重传节点没有通过全局容量挤掉另一个弱 NatServer。

| Relay 指标 | 实测 |
|---|---:|
| 采样数/周期 | 180 / 0.2s（约 36s） |
| 平均 CPU | 2.54% |
| 峰值 CPU | 10.00% |
| 空载 RSS | 15,916KiB |
| 峰值 RSS | 55,768KiB |
| 测试窗口 cache miss | +8,378 |
| 测试窗口 ACK 回收 | +8,360 |
| 测试窗口过期 | +562 |
| `quota_drop/eviction/mismatch/repeat_charged` | 全部 0 |

RSS 增长约 39MiB 是整个 Go Relay 进程的高水位，包含连接收发缓冲、frame route、Go heap 和缓存，
不能全部归因于指纹缓存；该轮日志中缓存可见条目低于约 1,200。Go 运行时测试后保留 heap，RSS 没有
立即回落属于预期，但仍应在长稳压测中监控 GC 与常驻高水位。

第二轮 NatServer2 的 CA 流量结算为 `7,353,202B`，相对 5MiB 应用载荷为 `1.402x`。其中本机日志
明确记录 3 个重传批次、263,418B；其余放大还混有 BNFS 帧内净荷、ACK 回收后的晚到帧以及双 TCP
failover 路径，不能把 CA 数字直接等同于 HTTP 文件大小。

### 公平性与弱网结论

1. **跨节点容量隔离通过本场景验证。** Pair A 的重传风暴没有造成 `quota_drop` 或 `eviction`，Pair B
   两轮均完整传输；未观察到“高流量 NatServer 把弱 NatServer 指纹挤掉后导致后者因容量重复计费”。
2. **极端弱网自身体验不通过。** `900ms/ACK` 高于 800ms 初始下限后形成重传雪崩，Pair A 吞吐降到
   约 4.3KiB/s并超时；重连还会在保证金窗口外再次扣费。这是传输层 ACK 策略风险，不是缓存锁竞争。
3. **安全优先语义符合预期。** 缓存不确定时选择重新计费，没有出现伪造 Retransmit 免费流量；测试中
   `mismatch=0`、`quota_drop=0`，精确命中才减免。
4. **按 goodput 精确收费仍有差距。** ACK 成功转发后立即批量回收会让随后在途的晚到重传变成 miss；
   dual-leg 重连也可能改变 Relay 内部 MessageID。两项都只会多计、不造成漏计，但对弱网不友好。

因此，当前版本可作为 BILL-RT-01 的安全修复，但不能宣称“极端弱网按 HTTP goodput 精确收费”。若
产品要改善弱网费用，下一步应在不扩大活跃缓存保护额度的前提下增加一个有界 ACK grace tombstone
层（建议最多 `2 * maxAckTimeout = 16s`，独立全局/节点配额、优先淘汰），并让同一逻辑消息跨 TCP
leg 使用稳定的 canonical MessageID。未完成这两项前，不建议把 `900ms/ACK` 场景的 3.0x 放大视为
可接受计费体验。

### 测试收尾与证据

- 所有本地 NatServer/NatClient/HTTP 测试进程已经停止；server4 Relay 保持已修复版本运行；
- 四个测试身份在最终结算后均精确恢复为 `209,715,200B`（200MiB）；
- 私钥仍只保存在 `.credentials/runtime-keys/` 和受限凭据文件中，报告不记录私钥内容；
- 原始本机日志位于 `.credentials/rt-cache-benchmark-20260711/`，Relay 证据副本为其中的
  `relay.log` 与 `relay-top2.log`；该目录被 gitignore，不应提交到仓库。

## 建议决策

若产品接受“按 relay 实际带宽收费”，优先采用方案 A，它具有最简单的安全边界和最低运行成本。
若希望为正常弱网提供有限保护，建议采用方案 B：初始免费比例 3%～5%，token 上限 1MiB/连接或
4MiB/节点，超出部分全额计费。

综合当前目标（relay 性能和网络安全优先，可以牺牲部分 NatServer 性能），建议在实地部署前将
当前指纹方案替换为方案 B；若计费模型允许弱网重传全部由 NatServer 承担，则直接采用方案 A。

## KCP/TCP 共存恢复与实机复测（2026-07-12）

### 修复内容

拨号器原本在 `kcpPriorityWindow=200ms` 到期时调用 `kcpCancel()`。真实 WAN 中若 KCP 在 200ms 后、
`dialHandshakeTimeout=1500ms` 内完成，健康 KCP 也会被提前取消，最终只能得到双 TCP。本次改为：

1. 200ms 只决定当前 preferred；已就绪 TCP 可先成为 preferred；
2. KCP 继续使用完整 1500ms 握手期限；随后成功则作为 backup 并入同一 `DualStream`；
3. KCP 真正失败后仍补第二条 TCP，保留原双 TCP 降级；
4. server3、server4 和本机 NAT 测试进程均移除 `BNFS_DISABLE_KCP=1`。

该低风险实现会等待两条并发拨号结果后才返回，因此 UDP 黑洞环境下新建连接仍增加约 1.5 秒延迟；
本次没有进一步改成异步 late attach。

新增 `TestDualDial_KCPLateWithinHandshake_CoexistsWithTCP`，用 `BNFS_TEST_ACK_DELAY=350ms` 强制
KCP 晚于 200ms、早于 1500ms 完成。实测 `legs=[tcp kcp]`、约 352ms；原
`TestDualDial_KCPBlocked_FallbackToSecondTCP` 仍得到 `legs=[tcp tcp#2]`、约 1.503s。

### 自动化回归

以下带竞态检测的今日相关回归通过（约 5.6 秒）：

```bash
GOCACHE=/tmp/bnfs-go-cache go test -race ./networkFrameWork \
  -run 'TestDualDial_|TestConnectionStabilityKCP|TestForwardHookRetransmit|TestGlobalRetransmitCacheConcurrent' \
  -count=1
```

`admission`、`cmd/caserver`、`p2pnode/impl/natnode`、`p2pnode/impl/relaynode` 和 tunnel mux 相关测试
也通过。全仓并行运行时 `TestRelayHashChainIsolation` 曾出现一次
`connection ... was not bootstrapped`；单独连续重跑 3 次全部通过，每次约 60.1 秒，暂归类为既有
时序偶发而不是稳定回归。`go vet` 仍有一个与本改动无关的既有问题：
`cmd/tunnel/test_validator/main.go:226` 的 `fmt.Println` 参数末尾包含冗余换行。

### 双服务器滚动部署

| 位置 | 服务 | 发布结果 |
|---|---|---|
| server3 `129.28.42.247` | CA Web `:9000` + index `:9010` | CA/账本未重启；只升级 index；TCP/UDP `:9010` 均监听 |
| server4 `193.112.62.9` | relay `:9000` | 升级 relay；TCP/UDP `:9000` 均监听；注册到 server3 成功 |

两机 `nodeserver` SHA-256 均为
`f7e7b298917e7bfa53bf80b89af09f48a2d001903f1f8256c49e33a7ba7aeaec`。server3 回滚文件为
`/root/bnfs-net/nodeserver.pre-kcp-dual-20260712-000650`，server4 回滚文件为
`/root/bnfs-net/nodeserver.pre-kcp-dual-20260712-000800`。最终进程环境均不含
`BNFS_DISABLE_KCP`。

server4 到 server3 的最终控制链路在 index 日志中表现为同一 `connId` 的两条连接：KCP session
的 `local=[::]:9010` 和 TCP 的 `local=10.6.0.16:9010`。本机四个 NAT 身份接入 server4 时也各自
观察到 KCP/TCP 两条注册连接，没有出现启动阶段的“双 TCP fallback”日志。

### 正常双腿数据面

清空 relay 内存态路由后，本机 NatServer1/NatClient1 经 server4 建立 KCP+TCP 隧道。5MiB 随机文件
在 6.855 秒内完整下载：文件大小 `5,242,880B`，源端与接收端 SHA-256 均为
`52293dab1217323af7bebf607516dd52f123a47d312dc09b9acdbb9d7a84cfc9`。该场景通过。

### 双 NatServer 弱网竞争复测

冷启动 relay 后，本机同时运行两组 NAT 节点，均启用 KCP+TCP：

- Pair A：正常 NatServer1 + `BNFS_TEST_ACK_DELAY=900ms` 的 NatClient1；
- Pair B：`BNFS_TEST_DATA_FRAME_DELAY=2ms` 的 NatServer2 + 正常 NatClient2。

| 场景 | 应用接收 | 用时 | SHA-256 | 结果 |
|---|---:|---:|---|---|
| Pair A：900ms/ACK client | 524,082B / 5MiB | 120.014s | 部分文件，不应匹配 | curl 超时，失败 |
| Pair B：2ms/帧 server | 5,242,880B | 21.670s | 与源端一致 | 成功 |

Pair A 的 client/server 日志分别记录 77/147 条 `retransmit-test` 事件，并在后段发生 KCP、TCP leg
重连；Pair B 完整传输。高重传 Pair A 没有挤垮共享同一 relay 的 Pair B。Relay 在 625 次、0.2 秒
间隔的 `ps` 采样中，进程 `%CPU` 读数平均 2.37%、最高 2.70%，RSS 最高 45,748KiB。这里的
`%CPU` 是 `ps` 的进程生命周期平均值采样，不是严格的窗口 CPU 利用率，不能当作容量承诺。

测试结束前缓存累计指标为：`entries=889`、`hit=1343`、`miss=11546`、`expired=327`、
`ack_removed=10330`；`mismatch/repeat_charged/quota_drop/eviction` 均为 0。结论与上一轮双 TCP 测试
一致：缓存配额隔离有效，但极端逐 ACK 延迟会造成重传雪崩；启用 KCP 并不能修复 ACK 策略本身。

### 实机发现的补充异常

#### P1：注册双腿全部断开后残留僵尸 StreamGroup

第一轮本机进程被测试执行环境清理后，relay 已记录两条注册腿 EOF，但没有删除对应 NAT 节点的
`StreamGroup`。同一身份重新上线时日志显示“附加 relay leg 到已有 StreamGroup”，随后 KCP 和 TCP
业务腿均在约 36 秒后因 `max retransmits exceeded` 失败，server 端收不到 TLS 握手。重启 server4
清空内存态 group 后，同一版本、同一身份立即成功，且 5MiB 校验通过。

这说明当前 relay 缺少“注册组最后一条 leg 退出时删除 group、natNodes 和旧 frame route”的完整
生命周期回收。正式长期运行前应增加 group generation/owner token，只有当前 generation 的最后一条
leg 才能原子删除；新注册应创建新 generation，不能继承旧 endpoint/routes。需要补充“同身份双腿
同时断开后立即重连”的回归测试。本轮通过在测试结束后重启 relay 清理现场，未修改该独立缺陷。

#### P1：单向 UDP 黑洞下 TCP backup 未接管

在已通过 5MiB 的隧道上，临时仅丢弃本机发往 `193.112.62.9:9000/UDP` 的报文。36.816 秒后 KCP
达到最大重传，但 curl 以 `Recv failure: Connection reset by peer` 结束，TCP backup 随后也等待 ACK
超时；防火墙规则已立即删除。

根因特征是 relay 已把该逻辑消息的回程 route 固定到 KCP。单向黑洞时 relay 本机 UDP `write` 仍然
成功，无法仅凭写错误发现远端 ACK 已断，因此不会及时把 route 迁移到 TCP。当前 failover 能覆盖
明确的本地写失败/连接关闭，不能覆盖“写成功但回程全丢”的半开 UDP。建议为每个 active route 增加
有界 ACK 进度 watchdog：连续 `maxAckTimeout` 无 ACK 进展时将整条消息按已有缓存重放到 TCP，并用
route epoch 丢弃旧 KCP 的晚到 ACK；必须先补单向丢包和 ACK 乱序测试，避免双路重复投递。

### 收尾状态

- server4 已再次冷重启，僵尸 NAT group 已清空；server3/server4 的 KCP+TCP 监听和控制链路正常；
- 本机所有 NAT/HTTP 测试进程均已停止，临时 UDP 丢弃规则不存在；
- 四个测试身份在最终结算后均恢复为 `209,715,200B`；私钥未写入报告；
- 原始证据位于 `.credentials/kcp-dual-live-20260712/`，该目录被 `.gitignore` 排除；
- 正常共存与正常传输通过；极端 ACK 弱网符合既有失败表现；两个 P1 异常未在本轮扩大范围修复。
